[package]
name = "bevy-game-test"
version = "0.1.0"
edition = "2021"

[features]
default = ["debug"]
debug = ["bevy-inspector-egui"]
fps = []

[dependencies]
bevy = "0.7"
bevy-inspector-egui = { version = "0.11", optional = true }
